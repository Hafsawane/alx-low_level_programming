#include "main.h"

/**
 * _strlen -returns the length of a string
* @s: string parameter
* Return: string Length
*/
int _strlen(char *s)
{
int j;
for (j = 0; s[j] != '\0'; j++)
{
continue;
}
return (j);
}
